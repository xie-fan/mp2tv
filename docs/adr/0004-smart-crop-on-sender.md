# 智能截取在手机端编码前完成

部分手机的硬件编码器不支持竖屏全分辨率（如 1080×2400），整屏画面要先缩小再编码；如果在电脑端裁剪，内容区域是先缩小再放大，画面会糊。所以手机端先裁出内容区域再编码，编码分辨率就是内容区域的原始像素。

## Consequences

- 内容区域变化时要重建编码器并发关键帧，会短暂卡顿，所以区域变化要先稳定一段时间再切换。
- Android 端需要一条 GPU 渲染管线来裁剪画面。
